# 开机自启全自动流程 Implementation Plan

> **For agentic workers:** Implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 开机自启拉起 OASX 后自动拉起 server、检测就绪、自动进主界面，并按每脚本配置的延时（相对就绪时刻 T0）错峰启动脚本；自启配置面板改为 server 可达即可配置。

**Architecture:** 新增 `AutoBootService`（所有平台注册，持有自启配置状态并编排全自动流程）与 `ServerLauncher`（从 ServerController 抽出的 Windows 专属 server 拉起逻辑）；`ScriptService` 剥离自启配置与无条件自启，改为 onInit 完成后回调 AutoBootService；配置面板改为 HTTP 探测 + `getScriptList` 数据源。

**Tech Stack:** Flutter 桌面（Windows 为主）、GetX（GetxService/Rx/GetStorage）、flutter_test。

**Spec:** `docs/superpowers/specs/2026-07-30-auto-boot-design.md`

## Global Constraints

- 所有新增/修改代码写中文注释；受改动影响的既有注释同步更新
- `delaySeconds` 取值 0–86400，非法值按 0 处理（spec §4.3）
- 后端地址取值规则：优先 `StorageKey.address`，未保存则默认 `127.0.0.1:22288`（spec §3）
- 就绪轮询间隔 2s、超时 5 分钟（spec §4.2）
- 自动拉起 server 仅 Windows；macOS/Linux 跳过拉起仅探测（spec §3）
- 触发条件：`--autostart` 参数 && 桌面平台 && 自启条目非空（spec §3）
- 存储 key 沿用 `StorageKey.autoScriptList`，读取兼容三种历史形态（spec §4.3）
- **git commit 前必须先向用户展示拟定 message 并获确认**（用户全局规则）——各 Task 的 Commit 步骤只准备 message 与暂存清单，实际提交统一等用户确认后执行

---

### Task 1: AutoScriptEntry 模型与存储解析

**Files:**
- Create: `lib/model/auto_script_entry.dart`
- Test: `test/model/auto_script_entry_test.dart`

**Interfaces:**
- Produces: `AutoScriptEntry`（`String name`、`int delaySeconds`、`toJson()`）、`AutoScriptEntry.parseStored(dynamic raw) → List<AutoScriptEntry>`、`AutoScriptEntry.encodeList(List<AutoScriptEntry>) → String`、常量 `AutoScriptEntry.maxDelaySeconds = 86400`

- [ ] **Step 1: 写失败测试**

```dart
// test/model/auto_script_entry_test.dart
import 'dart:convert';

import 'package:flutter_test/flutter_test.dart';
import 'package:oasx/model/auto_script_entry.dart';

// 中文注释：锁定自启条目的存储解析——兼容 raw List 直存 / JSON 字符串两种容器，
// 字符串元素（旧格式，延时 0）/ 对象元素（新格式）两种元素，及脏数据防护。
void main() {
  test('旧格式 raw List 直存迁移为延时 0 的条目', () {
    final entries = AutoScriptEntry.parseStored(['oas1', 'oas2']);
    expect(entries.map((e) => e.name), ['oas1', 'oas2']);
    expect(entries.every((e) => e.delaySeconds == 0), isTrue);
  });

  test('旧格式 JSON 字符串迁移为延时 0 的条目', () {
    final entries = AutoScriptEntry.parseStored(jsonEncode(['oas1']));
    expect(entries.single.name, 'oas1');
    expect(entries.single.delaySeconds, 0);
  });

  test('新格式对象数组解析出延时', () {
    final raw = jsonEncode([
      {'name': 'oas1', 'delaySeconds': 30},
      {'name': 'oas2', 'delaySeconds': 90},
    ]);
    final entries = AutoScriptEntry.parseStored(raw);
    expect(entries[0].delaySeconds, 30);
    expect(entries[1].delaySeconds, 90);
  });

  test('脏数据（非 JSON / 非 List / 空名 / 非法延时）安全处理', () {
    expect(AutoScriptEntry.parseStored('{broken'), isEmpty);
    expect(AutoScriptEntry.parseStored(42), isEmpty);
    expect(AutoScriptEntry.parseStored(null), isEmpty);
    // 空名条目丢弃；延时非数字按 0；负数按 0；超上限截断
    final entries = AutoScriptEntry.parseStored(jsonEncode([
      {'name': '', 'delaySeconds': 5},
      {'name': 'a', 'delaySeconds': 'x'},
      {'name': 'b', 'delaySeconds': -3},
      {'name': 'c', 'delaySeconds': 999999},
    ]));
    expect(entries.map((e) => e.name), ['a', 'b', 'c']);
    expect(entries.map((e) => e.delaySeconds),
        [0, 0, AutoScriptEntry.maxDelaySeconds]);
  });

  test('encodeList 输出新格式 JSON', () {
    final s = AutoScriptEntry.encodeList([AutoScriptEntry('oas1', 30)]);
    expect(jsonDecode(s), [
      {'name': 'oas1', 'delaySeconds': 30}
    ]);
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/model/auto_script_entry_test.dart`
Expected: 编译失败（`auto_script_entry.dart` 不存在）

- [ ] **Step 3: 最小实现**

```dart
// lib/model/auto_script_entry.dart
import 'dart:convert';

// 自动启动脚本条目：脚本名 + 相对后端就绪时刻(T0)的延时秒数。
// 存储于 StorageKey.autoScriptList，新格式为对象数组 JSON 字符串。
class AutoScriptEntry {
  final String name;
  final int delaySeconds;

  // 延时上限 24 小时（spec §4.3）
  static const int maxDelaySeconds = 86400;

  AutoScriptEntry(this.name, int delaySeconds)
      : delaySeconds = _clampDelay(delaySeconds);

  // 延时限定 0–86400，非法值按 0 处理
  static int _clampDelay(int value) {
    if (value < 0) return 0;
    if (value > maxDelaySeconds) return maxDelaySeconds;
    return value;
  }

  Map<String, dynamic> toJson() =>
      {'name': name, 'delaySeconds': delaySeconds};

  // 解析存储原始值，兼容三种历史形态：
  // 1) raw List 直存（最早期版本）2) JSON 字符串包 List（前一版）
  // 元素为字符串 → 旧格式，延时按 0；元素为对象 → 新格式。
  // 任何解析失败按空列表处理，空名条目丢弃。
  static List<AutoScriptEntry> parseStored(dynamic raw) {
    dynamic decoded = raw;
    if (raw is String) {
      if (raw.trim().isEmpty) return [];
      try {
        decoded = jsonDecode(raw);
      } catch (_) {
        return [];
      }
    }
    if (decoded is! List) return [];
    final entries = <AutoScriptEntry>[];
    for (final item in decoded) {
      if (item is String) {
        if (item.isNotEmpty) entries.add(AutoScriptEntry(item, 0));
      } else if (item is Map) {
        final name = item['name'];
        if (name is String && name.isNotEmpty) {
          final delay = item['delaySeconds'];
          entries.add(AutoScriptEntry(
              name, delay is int ? delay : int.tryParse('$delay') ?? 0));
        }
      }
    }
    return entries;
  }

  // 序列化为新格式 JSON 字符串（对象数组）
  static String encodeList(List<AutoScriptEntry> entries) =>
      jsonEncode(entries.map((e) => e.toJson()).toList());
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/model/auto_script_entry_test.dart`
Expected: 全部 PASS

- [ ] **Step 5: 准备 commit（暂不执行，见 Global Constraints）**

```
feat(autoboot): 新增 AutoScriptEntry 模型与存储三形态兼容解析
```

暂存：`lib/model/auto_script_entry.dart` `test/model/auto_script_entry_test.dart`

---

### Task 2: AutoBootService 配置状态与纯逻辑

**Files:**
- Create: `lib/service/auto_boot_service.dart`（本 Task 只写配置状态与纯函数，流程编排在 Task 5 补充）
- Modify: `lib/model/const/storage_key.dart:7`（注释更新为新格式说明）
- Test: `test/service/auto_boot_service_test.dart`

**Interfaces:**
- Consumes: Task 1 的 `AutoScriptEntry`
- Produces:
  - `AutoBootService extends GetxService`，字段 `autoScriptEntries: RxList<AutoScriptEntry>`
  - `void loadEntries()`（读存储+迁移写回）、`bool isSelected(String name)`、`int delayOf(String name)`（未勾选返回 0）
  - `void setSelected(String name, bool selected)`、`void setDelay(String name, int seconds)`、`void removeEntry(String name)` —— 均立即持久化
  - `static bool shouldAutoBoot({required bool hasAutostartArg, required bool isDesktop, required int entryCount})`
  - `static Duration remainingDelay(int delaySeconds, DateTime t0, DateTime now)` = `max(0, delaySeconds - (now - t0))`

- [ ] **Step 1: 写失败测试**

```dart
// test/service/auto_boot_service_test.dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter/services.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:get/get.dart';
import 'package:get_storage/get_storage.dart';
import 'package:oasx/model/auto_script_entry.dart';
import 'package:oasx/model/const/storage_key.dart';
import 'package:oasx/service/auto_boot_service.dart';

// 中文注释：测试 AutoBootService 的配置状态（读取迁移/增删改持久化）与纯逻辑
// （触发条件判定、剩余延时补偿）。GetStorage 依赖 path_provider，按既有模式 mock。
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUpAll(() async {
    Get.testMode = true;
    const channel = MethodChannel('plugins.flutter.io/path_provider');
    TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
        .setMockMethodCallHandler(channel, (MethodCall call) async {
      // 目录名与其他测试文件区分，避免并行 isolate 冲突
      return '${Directory.systemTemp.path}/oasx_test_storage_autoboot';
    });
    await GetStorage.init();
  });

  setUp(() async {
    await GetStorage().remove(StorageKey.autoScriptList.name);
  });

  test('loadEntries 迁移旧格式并按新格式写回', () async {
    await GetStorage()
        .write(StorageKey.autoScriptList.name, jsonEncode(['oas1']));
    final service = AutoBootService();
    service.loadEntries();
    expect(service.isSelected('oas1'), isTrue);
    expect(service.delayOf('oas1'), 0);
    // 写回后存储值应为新格式对象数组
    final raw = GetStorage().read(StorageKey.autoScriptList.name);
    expect(jsonDecode(raw as String), [
      {'name': 'oas1', 'delaySeconds': 0}
    ]);
  });

  test('setSelected/setDelay/removeEntry 更新状态并持久化', () async {
    final service = AutoBootService();
    service.loadEntries();
    service.setSelected('oas2', true);
    service.setSelected('oas1', true);
    // 条目保持稳定排序（按名称）
    expect(service.autoScriptEntries.map((e) => e.name), ['oas1', 'oas2']);
    service.setDelay('oas2', 90);
    expect(service.delayOf('oas2'), 90);
    service.setSelected('oas1', false);
    expect(service.isSelected('oas1'), isFalse);
    service.removeEntry('oas2');
    expect(service.autoScriptEntries, isEmpty);
    expect(jsonDecode(GetStorage().read(StorageKey.autoScriptList.name)),
        isEmpty);
  });

  test('shouldAutoBoot 三条件缺一不可', () {
    expect(
        AutoBootService.shouldAutoBoot(
            hasAutostartArg: true, isDesktop: true, entryCount: 1),
        isTrue);
    expect(
        AutoBootService.shouldAutoBoot(
            hasAutostartArg: false, isDesktop: true, entryCount: 1),
        isFalse);
    expect(
        AutoBootService.shouldAutoBoot(
            hasAutostartArg: true, isDesktop: false, entryCount: 1),
        isFalse);
    expect(
        AutoBootService.shouldAutoBoot(
            hasAutostartArg: true, isDesktop: true, entryCount: 0),
        isFalse);
  });

  test('remainingDelay 按 T0 补偿且不为负', () {
    final t0 = DateTime(2026, 7, 30, 8, 0, 0);
    // 就绪后 10 秒调度、延时 30 秒 → 剩余 20 秒
    expect(
        AutoBootService.remainingDelay(
            30, t0, t0.add(const Duration(seconds: 10))),
        const Duration(seconds: 20));
    // 已超时 → 0（立即启动）
    expect(
        AutoBootService.remainingDelay(
            30, t0, t0.add(const Duration(seconds: 60))),
        Duration.zero);
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/service/auto_boot_service_test.dart`
Expected: 编译失败（`auto_boot_service.dart` 不存在）

- [ ] **Step 3: 最小实现**

```dart
// lib/service/auto_boot_service.dart
import 'package:get/get.dart';
import 'package:get_storage/get_storage.dart';
import 'package:oasx/model/auto_script_entry.dart';
import 'package:oasx/model/const/storage_key.dart';

// 开机自启全自动流程编排者 + 自启脚本配置状态持有者。
// 所有平台注册（配置面板登录前即需数据源）；流程编排仅桌面且 --autostart 时执行，
// 见 spec docs/superpowers/specs/2026-07-30-auto-boot-design.md。
class AutoBootService extends GetxService {
  final _storage = GetStorage();

  // 自启脚本条目（勾选即存在条目），持久化于 StorageKey.autoScriptList
  final autoScriptEntries = <AutoScriptEntry>[].obs;

  // 从存储读取并迁移：解析后按新格式写回（幂等）；
  // 首次运行（无存储值）不写回，避免无意义的空数组落盘
  void loadEntries() {
    final raw = _storage.read(StorageKey.autoScriptList.name);
    autoScriptEntries.value = AutoScriptEntry.parseStored(raw);
    if (raw != null) _persist();
  }

  // 按新格式（对象数组 JSON 字符串）写入存储
  void _persist() {
    _storage.write(StorageKey.autoScriptList.name,
        AutoScriptEntry.encodeList(autoScriptEntries.toList()));
  }

  bool isSelected(String name) =>
      autoScriptEntries.any((e) => e.name == name);

  // 未勾选返回 0
  int delayOf(String name) => autoScriptEntries
      .firstWhereOrNull((e) => e.name == name)
      ?.delaySeconds ??
      0;

  // 勾选/取消勾选；勾选新增延时 0 的条目，保持按名称稳定排序
  void setSelected(String name, bool selected) {
    if (selected) {
      if (!isSelected(name)) {
        autoScriptEntries.add(AutoScriptEntry(name, 0));
        autoScriptEntries.sort((a, b) => a.name.compareTo(b.name));
      }
    } else {
      autoScriptEntries.removeWhere((e) => e.name == name);
    }
    _persist();
  }

  // 修改延时（条目不存在则忽略；越界由 AutoScriptEntry 构造钳制）
  void setDelay(String name, int seconds) {
    final index = autoScriptEntries.indexWhere((e) => e.name == name);
    if (index < 0) return;
    autoScriptEntries[index] = AutoScriptEntry(name, seconds);
    _persist();
  }

  // 脚本被删除时同步移除自启条目（由 ScriptService.deleteScriptModel 调用）
  void removeEntry(String name) {
    final before = autoScriptEntries.length;
    autoScriptEntries.removeWhere((e) => e.name == name);
    if (autoScriptEntries.length != before) _persist();
  }

  // 触发条件：--autostart 参数 && 桌面平台 && 自启条目非空（spec §3）
  static bool shouldAutoBoot(
      {required bool hasAutostartArg,
      required bool isDesktop,
      required int entryCount}) {
    return hasAutostartArg && isDesktop && entryCount > 0;
  }

  // 剩余延时 = max(0, delaySeconds - (now - T0))，T0 为后端就绪时刻
  static Duration remainingDelay(
      int delaySeconds, DateTime t0, DateTime now) {
    final remain = Duration(seconds: delaySeconds) - now.difference(t0);
    return remain.isNegative ? Duration.zero : remain;
  }
}
```

同时更新 `lib/model/const/storage_key.dart` 注释：

```dart
  // 自动启动脚本条目，存储对象数组 JSON（[{"name":..,"delaySeconds":..}]），
  // 兼容读取历史的脚本名数组格式
  autoScriptList,
```

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/service/auto_boot_service_test.dart`
Expected: 全部 PASS

- [ ] **Step 5: 准备 commit**

```
feat(autoboot): AutoBootService 配置状态、迁移写回与触发/延时纯逻辑
```

---

### Task 3: ServerLauncher 抽取（Windows 专属拉起逻辑复用）

**Files:**
- Create: `lib/service/server_launcher.dart`
- Modify: `lib/controller/server/server_controller.dart:46-117`（`authenticatePath` 与 `run()` 改为委托）
- Test: `test/service/server_launcher_test.dart`

**Interfaces:**
- Consumes: 无（独立工具类）
- Produces:
  - `ServerLauncher.validatePath(String root) → bool`（静态，原 `authenticatePath` 逻辑迁移）
  - `ServerLauncher({required String rootPath, required void Function(String) onLog})`
  - `Future<void> launch()`（原 `run()` 命令序列：installer → taskkill pythonw → pythonw server.py，含既有「杀掉所有 pythonw」副作用）
  - `void kill()`（终止本实例 shell 中未完成的命令；重复启动前先 kill 旧实例，保持原「单 shell 复用 + kill」语义）

- [ ] **Step 1: 写失败测试**

```dart
// test/service/server_launcher_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:oasx/service/server_launcher.dart';

// 中文注释：测试 ServerLauncher.validatePath 的目录结构校验（原
// ServerController.authenticatePath 逻辑迁移）。launch() 涉及真实 shell，
// 不做单测，由端到端手动验证覆盖。
void main() {
  test('validatePath 校验 toolkit/git/installer/deploy 缺一不可', () async {
    final root = Directory.systemTemp.createTempSync('oasx_launcher_test');
    addTearDown(() => root.deleteSync(recursive: true));

    // 空目录 → false
    expect(ServerLauncher.validatePath(root.path), isFalse);

    // 补齐全部标记文件 → true
    File('${root.path}/toolkit/python.exe').createSync(recursive: true);
    File('${root.path}/toolkit/Git/cmd/git.exe').createSync(recursive: true);
    File('${root.path}/deploy/installer.py').createSync(recursive: true);
    File('${root.path}/config/deploy.yaml').createSync(recursive: true);
    expect(ServerLauncher.validatePath(root.path), isTrue);

    // 不存在的根目录 → false
    expect(ServerLauncher.validatePath('${root.path}/nope'), isFalse);
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/service/server_launcher_test.dart`
Expected: 编译失败（`server_launcher.dart` 不存在）

- [ ] **Step 3: 实现 ServerLauncher**

```dart
// lib/service/server_launcher.dart
import 'dart:io';

import 'package:get/get.dart';
import 'package:process_run/shell.dart';

// server 拉起器：Server 页手动启动与开机自启自动流程共用。
// 命令序列为 Windows 专属（pythonw.exe / taskkill），从 ServerController 抽出；
// 保留既有副作用：taskkill 会杀掉系统内所有 pythonw 进程。
class ServerLauncher {
  final String rootPath;
  // 日志回流（Server 页日志区或 AutoBootService 的 printInfo）
  final void Function(String line) onLog;

  Shell? _shell;
  final _linesController = ShellLinesController();

  ServerLauncher({required this.rootPath, required this.onLog}) {
    _linesController.stream.listen((event) => onLog('INFO: $event'));
    _shell = _buildShell();
  }

  // 校验 OAS 根目录结构（原 ServerController.authenticatePath 逻辑迁移）：
  // 依次检查根目录、toolkit/python.exe、toolkit/Git/cmd/git.exe、
  // deploy/installer.py、config/deploy.yaml
  static bool validatePath(String root) {
    try {
      if (!Directory(root).existsSync()) return false;
      if (!File('$root/toolkit/python.exe').existsSync()) return false;
      if (!File('$root/toolkit/Git/cmd/git.exe').existsSync()) return false;
      if (!File('$root/deploy/installer.py').existsSync()) return false;
      if (!File('$root/config/deploy.yaml').existsSync()) return false;
    } catch (_) {
      return false;
    }
    return true;
  }

  // PATH 环境（原 ServerController 各 path getter 迁移）
  String get _pathGit => '$rootPath\\toolkit\\Git\\mingw64\\bin"';
  String get _pathPython => '$rootPath\\toolkit';
  String get _pathAdb =>
      '$rootPath\\toolkit\\Lib\\site-packages\\adbutils\\binaries';
  String get _pathScripts => '$rootPath\\toolkit\\Scripts';
  Map<String, String> get _pathEnv => {
        'PATH': '$rootPath,$_pathGit,$_pathPython,$_pathAdb,$_pathScripts'
      };

  Shell _buildShell() => Shell(
        workingDirectory: rootPath,
        runInShell: true,
        environment: _pathEnv,
        stdout: _linesController.sink,
        verbose: false,
      );

  Future<void> _runShell(String command) async {
    try {
      await _shell!.run(command);
    } on ShellException catch (e) {
      onLog('ERROR: ${e.toString()}');
    }
  }

  // 拉起 server（原 ServerController.run 命令序列，fire-and-forget 风格保留）
  Future<void> launch() async {
    _shell!.kill();
    _runShell('echo OAS working directory: ');
    _runShell('pwd');
    _runShell('python -m deploy.installer');
    _runShell('echo Start OAS');
    _runShell('taskkill /f /t /im pythonw.exe');
    _runShell(".\\toolkit\\pythonw.exe  server.py");
  }

  // 终止本实例 shell 中未完成的命令（重复启动前调用，避免旧命令进程残留）
  void kill() {
    _shell?.kill();
  }
}
```

- [ ] **Step 4: ServerController 委托**

`lib/controller/server/server_controller.dart` 改动点：

1. `authenticatePath(String root)` 方法体替换为 `return ServerLauncher.validatePath(root);`（方法保留，调用点不变），原实现删除
2. 删除字段 `shell`、`shellController` 与方法 `runShell`、getter `pathGit/pathPython/pathAdb/pathScripts/pathPATH/getShell`（原本只服务于 `run()`，日志改经 ServerLauncher 的 `onLog` 回流）；`onInit`/`updateRootPathServer` 中 `shell = getShell` 与 `shellController.stream.listen` 相关行删除
3. 新增字段并改写 `run()`（持有实例复用，保持原「先 kill 旧命令再启动」语义）：

```dart
  // server 拉起器实例：重复启动前先 kill 旧实例，避免旧命令进程残留
  ServerLauncher? _launcher;

  void run() {
    clearLog();
    // 拉起逻辑抽至 ServerLauncher，与开机自启自动流程共用；日志仍回本页
    _launcher?.kill();
    _launcher = ServerLauncher(rootPath: rootPathServer.value, onLog: addLog);
    _launcher!.launch();
  }
```

4. `server_view.dart`（library server 机制）加 import：`import 'package:oasx/service/server_launcher.dart';`

- [ ] **Step 5: 运行测试 + 回归**

Run: `flutter test test/service/server_launcher_test.dart && flutter analyze`
Expected: 测试 PASS；analyze 无新增告警

- [ ] **Step 6: 准备 commit**

```
refactor(server): 抽取 ServerLauncher 供手动启动与自动流程共用
```

---

### Task 4: ScriptService 剥离自启配置与无条件自启

**Files:**
- Modify: `lib/service/script_service.dart`
- Modify: `lib/main.dart:75-91`（`initService` 注册 `AutoBootService`）
- Test: 更新 `test/service/script_service_auto_test.dart`

**Interfaces:**
- Consumes: Task 2 的 `AutoBootService`（`removeEntry`、`loadEntries`、`autoScriptEntries`）
- Produces:
  - `ScriptService` 不再持有 `autoScriptList` / `updateAutoScript` / `autoRunScript` / `_waitBackendReadyAndAutoRun`（全部删除）
  - `ScriptService.onInit` 末尾回调 `AutoBootService.onScriptServiceReady()`（Task 5 实现该方法，本 Task 先以空方法占位）
  - 保留 `isRunning(String name)`、`startScript(String name)`、`scriptModelMap`

- [ ] **Step 1: 更新测试（先改测试再改实现）**

`test/service/script_service_auto_test.dart` 重写：删除 `updateAutoScript`/`restore`/`autoRunScript` 三个用例（职责已移至 `AutoBootService`，Task 2 已覆盖），保留 `isRunning` 用例，新增「deleteScriptModel 同步移除自启条目」用例：

```dart
// test/service/script_service_auto_test.dart
import 'dart:io';

import 'package:flutter/services.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:get/get.dart';
import 'package:get_storage/get_storage.dart';
import 'package:oasx/model/const/storage_key.dart';
import 'package:oasx/model/script_model.dart';
import 'package:oasx/service/auto_boot_service.dart';
import 'package:oasx/service/script_service.dart';
import 'package:oasx/service/websocket_service.dart';

// 中文注释：ScriptService 自启相关职责已移至 AutoBootService（见
// docs/superpowers/specs/2026-07-30-auto-boot-design.md），本文件保留
// isRunning 与「删除脚本同步移除自启条目」两个契约。
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUpAll(() async {
    Get.testMode = true;
    const channel = MethodChannel('plugins.flutter.io/path_provider');
    TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
        .setMockMethodCallHandler(channel, (MethodCall call) async {
      return '${Directory.systemTemp.path}/oasx_test_storage';
    });
    await GetStorage.init();
  });

  setUp(() async {
    await GetStorage().remove(StorageKey.autoScriptList.name);
    if (!Get.isRegistered<AutoBootService>()) {
      Get.put<AutoBootService>(AutoBootService(), permanent: true);
    }
    Get.find<AutoBootService>().loadEntries();
  });

  test('isRunning reflects scriptModelMap running state', () async {
    final service = _buildScriptService();
    service.addScriptModel('oas1');
    expect(service.isRunning('oas1'), isFalse);
    service.scriptModelMap['oas1']!.update(state: ScriptState.running);
    expect(service.isRunning('oas1'), isTrue);
  });

  test('deleteScriptModel 同步移除 AutoBootService 自启条目', () async {
    final autoBoot = Get.find<AutoBootService>();
    autoBoot.setSelected('oas1', true);
    final service = _buildScriptService();
    service.addScriptModel('oas1');
    service.deleteScriptModel('oas1');
    expect(autoBoot.isSelected('oas1'), isFalse);
  });
}

// 中文注释：构造 ScriptService 前 Get.put 一个无副作用的 WebSocketService，
// 绕开 ScriptService 字段初始化里 Get.find<WebSocketService> 的依赖。
ScriptService _buildScriptService() {
  if (!Get.isRegistered<WebSocketService>()) {
    Get.put<WebSocketService>(WebSocketService(), permanent: true);
  }
  return ScriptService();
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/service/script_service_auto_test.dart`
Expected: FAIL（`deleteScriptModel` 尚未接 AutoBootService）

- [ ] **Step 3: 改造 ScriptService**

`lib/service/script_service.dart` 改动点（对照现文件）：

1. 删除字段 `_storage`、`autoScriptList`、`_autoRunSampleTimer`；删除方法 `restoreAutoScriptListForTest`、`_loadAutoScriptListFromStorage`、`_persistAutoScriptList`、`updateAutoScript`、`_waitBackendReadyAndAutoRun`、`autoRunScript`；删除相应 import（`dart:convert`、`get_storage`、`storage_key`、`visibleForTesting`、`i18n_content`、`overview_view` 若仍被 `onClose` 使用则保留）
2. `onInit` 改为：

```dart
  @override
  Future<void> onInit() async {
    final scriptList = await ApiClient().getScriptList();
    if (scriptList.isNotEmpty) {
      await Future.wait(scriptList.map((name) => connectScript(name)));
    }
    super.onInit();
    // 脚本连接完成后通知自动流程编排者（非自动流程时该回调为 no-op）；
    // 自动启动脚本的触发与延时调度全部由 AutoBootService 负责
    if (Get.isRegistered<AutoBootService>()) {
      Get.find<AutoBootService>().onScriptServiceReady();
    }
  }
```

3. `onClose` 删除 `_autoRunSampleTimer` 相关两行
4. `deleteScriptModel` 中自启标记同步改为：

```dart
    // 同步移除自启条目（覆盖真实删除场景；重命名场景由 renameConfig 单独迁移）
    if (Get.isRegistered<AutoBootService>()) {
      Get.find<AutoBootService>().removeEntry(name);
    }
```

5. `AutoBootService` 加占位方法（Task 5 填实现）：

```dart
  // ScriptService 完成脚本连接后的回调；自动流程激活时触发延时调度（Task 5 实现）
  void onScriptServiceReady() {}
```

6. `lib/main.dart` `initService()` 中 `Get.lazyPut(() => WebSocketService());` 之前加：

```dart
  // 自启配置状态所有平台可用（配置面板登录前即需数据源）；
  // 自动流程本身仅桌面且 --autostart 时执行
  Get.put<AutoBootService>(AutoBootService()..loadEntries(), permanent: true);
```

并加 import `package:oasx/service/auto_boot_service.dart`。

- [ ] **Step 4: 迁移 renameConfig 的自启标记迁移逻辑**

`lib/controller/ctrl_nav.dart:146-177`（`NavCtrl.renameConfig`）现引用 `scriptService.autoScriptList` 与 `updateAutoScript`，改为经 `AutoBootService` 且保留延时值：

```dart
    // 删旧前先捕获自启标记与延时，rename 语义下走显式原子迁移
    // （deleteScriptModel 内部也会移除自启条目，但 try/catch 吞异常可能半迁移，
    // 故此处不依赖副作用，迁移步骤在末尾显式完成）
    final autoBoot = Get.find<AutoBootService>();
    final hadAutoRun = autoBoot.isSelected(oldName);
    final oldDelay = autoBoot.delayOf(oldName);
```

末尾迁移块改为：

```dart
    // 显式原子迁移：先移除旧名（若 deleteScriptModel 副作用未生效则补一次），
    // 再添加新名并还原延时，保证条目不出现 oldName+newName 同时残留
    if (hadAutoRun) {
      autoBoot.removeEntry(oldName);
      autoBoot.setSelected(newName, true);
      autoBoot.setDelay(newName, oldDelay);
    }
```

并加 import `package:oasx/service/auto_boot_service.dart`。改完 `grep -rn "autoScriptList\|updateAutoScript" lib/` 确认除 `storage_key.dart` 的 enum 定义与 `AutoBootService` 内部外无残留引用。

- [ ] **Step 5: 面板最小绑定迁移（保证本 Task 结束时可编译可测）**

`lib/views/server/auto_start_settings.dart` 的 `_buildScriptList()` 仍引用已删除的 `autoScriptList`/`updateAutoScript`，本 Task 先做**最小迁移**（UI 结构不动，仅换数据源，完整改造在 Task 6）：

```dart
  // 自动启动脚本多选列表（临时形态：数据源仍为 scriptModelMap，
  // 勾选绑定已移交 AutoBootService；Task 6 将整体改为 HTTP 探测 + 延时输入）
  Widget _buildScriptList() {
    final scriptService = Get.find<ScriptService>();
    final autoBoot = Get.find<AutoBootService>();
    return Obx(() {
      final scriptNames = scriptService.scriptModelMap.keys.toList();
      // 读取 autoScriptEntries 建立响应式依赖
      final selected = autoBoot.autoScriptEntries.map((e) => e.name).toSet();
      return Column(
        children: scriptNames
            .map((name) => CheckboxListTile(
                  value: selected.contains(name),
                  onChanged: (v) => autoBoot.setSelected(name, v ?? false),
                  title: Text(name),
                  dense: true,
                  controlAffinity: ListTileControlAffinity.leading,
                ))
            .toList(),
      );
    });
  }
```

同步最小更新 `test/views/server/auto_start_settings_test.dart`：setUp 注册 `AutoBootService`（`Get.put<AutoBootService>(AutoBootService()..loadEntries(), permanent: true)`，tearDown 删除）；「勾选触发」用例的持久化断言从 `jsonEncode(['oas1'])` 改为新格式：

```dart
    expect(jsonDecode(GetStorage().read(StorageKey.autoScriptList.name)), [
      {'name': 'oas1', 'delaySeconds': 0}
    ]);
```

断言勾选状态改为 `Get.find<AutoBootService>().isSelected('oas1')`。

- [ ] **Step 6: 运行测试确认通过**

Run: `flutter test test/service/script_service_auto_test.dart test/views/server/auto_start_settings_test.dart && flutter analyze`
Expected: PASS；无新增告警

- [ ] **Step 7: 准备 commit**

```
refactor(script): 自启配置与自动启动职责移交 AutoBootService
```

---

### Task 5: 自动流程编排（--autostart 参数、拉起、就绪、登录、延时调度）

**Files:**
- Modify: `lib/service/auto_boot_service.dart`（补流程编排）
- Modify: `lib/main.dart`（`main(List<String> args)` 透传参数、触发 `start()`）
- Modify: `lib/controller/login/login_controller.dart:17-20`（自动登录让位）
- Modify: `lib/controller/settings.dart:37-49`（killServer 取消调度）
- Modify: `lib/service/auto_start_service.dart`（三平台注册项加 `--autostart`）
- Test: `test/service/auto_start_service_args_test.dart`（新增）

**Interfaces:**
- Consumes: Task 2/3/4 的 `AutoBootService` 状态方法、`ServerLauncher`、`ScriptService.startScript/isRunning`
- Produces:
  - `AutoBootService.hasAutostartArg`（bool，由 main 注入）、`flowActive`（bool getter，LoginController 依赖）
  - `Future<void> start()`（幂等）、`void onScriptServiceReady()`（填实 Task 4 的占位）、`void cancelScheduled()`（killServer 依赖）
  - `AutoStartService.autostartArgument = '--autostart'`（静态常量，注册项与 main 解析共用）

- [ ] **Step 1: 写 AutoStartService 注册项参数的失败测试**

三平台注册内容构造方法目前是私有的，为可测加 `@visibleForTesting` 暴露（不改变行为）：`_buildWindowsTaskXml` → `buildWindowsTaskXml`（加 `@visibleForTesting`），`_buildMacPlist` → `buildMacPlist`，`_buildLinuxDesktopEntry` → `buildLinuxDesktopEntry`（文件内调用点同步改名）。

```dart
// test/service/auto_start_service_args_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:oasx/service/auto_start_service.dart';

// 中文注释：锁定三平台开机自启注册项都携带 --autostart 参数
// （spec §4.1：开机自启拉起 OASX 时以该参数区分于手动打开）。
// fake 跳过 onInit 的 PackageInfo/schtasks 副作用，直接测内容构造。
class _FakeAutoStartService extends AutoStartService {
  @override
  // ignore: must_call_super
  Future<void> onInit() async {}
}

void main() {
  test('Windows 计划任务 XML 携带 --autostart 参数', () {
    final xml = _FakeAutoStartService().buildWindowsTaskXml();
    expect(xml, contains('<Arguments>--autostart</Arguments>'));
  });

  test('注册项参数常量与解析端一致', () {
    expect(AutoStartService.autostartArgument, '--autostart');
  });
}
```

注：mac plist / linux desktop 的构造方法依赖 `onInit` 里初始化的 `_appName/_packageName`（`late final`），fake 跳过 onInit 后访问会抛错，故单测只覆盖 Windows XML（当前唯一实际部署平台）；mac/linux 改动靠 Step 3 代码评审与 analyze 保障。

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/service/auto_start_service_args_test.dart`
Expected: 编译失败（`buildWindowsTaskXml`/`autostartArgument` 不存在）

- [ ] **Step 3: AutoStartService 三平台注册项加参数**

`lib/service/auto_start_service.dart`：

1. 类顶加常量：

```dart
  // 开机自启拉起 OASX 的标识参数：注册项写入、main() 解析，两端必须一致
  static const String autostartArgument = '--autostart';
```

2. `buildWindowsTaskXml()`（原 `_buildWindowsTaskXml`，加 `@visibleForTesting` 与 `import 'package:flutter/foundation.dart' show visibleForTesting;`）的 `<Exec>` 块改为：

```xml
    <Exec>
      <Command>$exe</Command>
      <Arguments>$autostartArgument</Arguments>
    </Exec>
```

3. `buildMacPlist()` 的 `ProgramArguments` 数组补一项：

```xml
    <array>
      <string>$executable</string>
      <string>$autostartArgument</string>
    </array>
```

4. `buildLinuxDesktopEntry()` 的 Exec 行改为 `Exec=$executable $autostartArgument`
5. 注释注明：已有用户需重新关开一次「开机自启」开关，注册项才会带上参数（spec §4.1）

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/service/auto_start_service_args_test.dart`
Expected: PASS

- [ ] **Step 5: AutoBootService 补流程编排**

`lib/service/auto_boot_service.dart` 追加（import 补 `dart:async`、`api_client.dart`、`script_service.dart`、`server_launcher.dart`、`storage_key` 已有、`platform_utils.dart`、`get_storage` 已有）：

```dart
  // ---------------- 自动流程编排（spec §4.2） ----------------

  // 流程状态：idle 未触发；后续状态单向推进，failed/done 为终态
  static const _stateIdle = 'idle';
  static const _stateRunning = 'running'; // startingServer/waitingReady/loggingIn 合并为运行中
  static const _stateScheduling = 'scheduling';
  static const _stateDone = 'done';
  static const _stateFailed = 'failed';
  String _flowState = _stateIdle;

  // 是否携带 --autostart 启动参数（main() 注入）
  bool hasAutostartArg = false;

  // 自动流程是否激活：LoginController 据此让位自动登录
  bool get flowActive =>
      _flowState != _stateIdle && _flowState != _stateFailed;

  // 后端就绪时刻 T0（延时计时起点）
  DateTime? _readyAt;

  // 延时启动 Timer 统一持有，onClose/killServer 时取消
  final List<Timer> _pendingTimers = [];
  Timer? _sampleTimer;

  // 就绪轮询参数（spec：2s 间隔、5 分钟超时）
  static const _pollInterval = Duration(seconds: 2);
  static const _pollTimeout = Duration(minutes: 5);

  // 地址取值规则（spec §3）：优先已保存登录地址，无则默认
  String resolveAddress() {
    final saved = _storage.read(StorageKey.address.name);
    if (saved is String && saved.trim().isNotEmpty) return saved.trim();
    return '127.0.0.1:22288';
  }

  // 入口：满足触发条件才推进（幂等，重复调用直接返回）
  Future<void> start() async {
    if (_flowState != _stateIdle) return;
    if (!shouldAutoBoot(
        hasAutostartArg: hasAutostartArg,
        isDesktop: PlatformUtils.isDesktop,
        entryCount: autoScriptEntries.length)) {
      return;
    }
    _flowState = _stateRunning;
    try {
      ApiClient().setAddress('http://${resolveAddress()}');
      // 1. 先探测：server 可能已在运行（开机自启的后端服务等）
      var ready = await ApiClient().testAddress();
      if (!ready) {
        // 2. 仅 Windows 自动拉起 server；mac/linux 降级为只探测（spec §3）
        if (GetPlatform.isWindows) {
          final rootPath =
              (_storage.read('rootPathServer') as String?) ?? '';
          if (!ServerLauncher.validatePath(rootPath)) {
            _fail('OAS root path invalid, skip auto boot');
            return;
          }
          // printInfo 是 GetX 命名参数扩展，需包一层适配 onLog 的位置参数签名
          await ServerLauncher(
                  rootPath: rootPath,
                  onLog: (line) => printInfo(info: 'AutoBoot: $line'))
              .launch();
        }
        // 3. 轮询就绪（2s 间隔、5 分钟超时）
        ready = await _pollUntilReady();
      }
      if (!ready) {
        _fail('server not ready in ${_pollTimeout.inMinutes} minutes');
        return;
      }
      // 4. 首次可达时刻记为 T0；跳转主界面（地址已 set、可达已确认，
      //    与 LoginController.login 成功路径等效）
      _readyAt = DateTime.now();
      _flowState = _stateScheduling;
      if (Get.currentRoute != '/main') {
        Get.offAllNamed('/main');
      }
      // 5. 等 ScriptService 连接完成回调 onScriptServiceReady 触发调度
    } catch (e) {
      _fail('auto boot failed: $e');
    }
  }

  Future<bool> _pollUntilReady() async {
    final deadline = DateTime.now().add(_pollTimeout);
    while (DateTime.now().isBefore(deadline)) {
      if (await ApiClient().testAddress()) return true;
      await Future.delayed(_pollInterval);
    }
    return false;
  }

  void _fail(String reason) {
    _flowState = _stateFailed;
    printError(info: 'AutoBoot: $reason');
    Get.snackbar('AutoBoot', reason);
  }

  // ScriptService 完成脚本连接后的回调；仅 scheduling 态才执行延时调度，
  // 手动登录（流程未激活）时为 no-op —— 手动登录不再自动启动脚本
  void onScriptServiceReady() {
    if (_flowState != _stateScheduling || _readyAt == null) return;
    _flowState = _stateDone;
    _scheduleAutoRun(_readyAt!);
  }

  // 按各脚本延时（相对 T0 补偿）设 Timer 依次启动（spec §4.2 步骤 6）
  void _scheduleAutoRun(DateTime t0) {
    final scriptService = Get.find<ScriptService>();
    final entries = autoScriptEntries.toList();
    if (entries.isEmpty) return;
    var maxDelay = Duration.zero;
    for (final entry in entries) {
      final delay = remainingDelay(entry.delaySeconds, t0, DateTime.now());
      if (delay > maxDelay) maxDelay = delay;
      _pendingTimers.add(Timer(delay, () {
        // 已运行的跳过；后端侧已删除（不在 scriptModelMap）的跳过并记日志
        if (!scriptService.scriptModelMap.containsKey(entry.name)) {
          printError(info: 'AutoBoot: ${entry.name} not found, skip');
          return;
        }
        if (scriptService.isRunning(entry.name)) return;
        scriptService.startScript(entry.name).catchError((e) {
          // 到点时 server 不可用：记日志 + 提示，跳过不重试（spec §5）
          printError(info: 'AutoBoot: start ${entry.name} failed: $e');
          Get.snackbar(I18n.autoRunScript.tr, '${entry.name} failed');
        });
      }));
    }
    // 最晚一个脚本到点后再延迟采样运行状态，成功才提示（沿用原 autoRunScript 语义）
    _sampleTimer = Timer(maxDelay + const Duration(seconds: 4), () {
      final ok = entries
          .map((e) => e.name)
          .where(scriptService.isRunning)
          .toList();
      if (ok.isNotEmpty) {
        Get.snackbar(I18n.autoRunScript.tr, '$ok ${I18n.startSuccess.tr}');
      }
    });
  }

  // 取消全部未触发的延时启动（killServer / 服务销毁时调用，spec §5）
  void cancelScheduled() {
    for (final t in _pendingTimers) {
      t.cancel();
    }
    _pendingTimers.clear();
    _sampleTimer?.cancel();
    _sampleTimer = null;
  }

  @override
  void onClose() {
    cancelScheduled();
    super.onClose();
  }
```

import 补充 `package:oasx/config/translation/i18n_content.dart`（I18n 常量）。

- [ ] **Step 6: main.dart 透传参数并触发流程**

`lib/main.dart`：

```dart
void main(List<String> args) async {
  WidgetsFlutterBinding.ensureInitialized();
  await initService();
  // 开机自启拉起（注册项携带 --autostart）时标记自动流程；
  // 触发时机放在首帧后，确保 GetMaterialApp 路由可用
  Get.find<AutoBootService>().hasAutostartArg =
      args.contains(AutoStartService.autostartArgument);
  WidgetsBinding.instance.addPostFrameCallback((_) {
    Get.find<AutoBootService>().start();
  });

  runApp(
    ...
  );
}
```

import 已有 `auto_start_service.dart`，`auto_boot_service.dart` 在 Task 4 已加。

- [ ] **Step 7: LoginController 自动登录让位**

`lib/controller/login/login_controller.dart` `onInit` 的自动登录分支改为：

```dart
    // 自动流程激活时让位（server 可能未起，此时自动登录必然失败弹错；
    // 登录跳转时机由 AutoBootService 全权掌控，spec §4.2）
    final autoBootActive = Get.isRegistered<AutoBootService>() &&
        Get.find<AutoBootService>().flowActive;
    if (address.value.isNotEmpty && !logined && !autoBootActive) {
      logined = true;
      await login(address.value);
    }
```

login_view.dart（library login 机制）加 import `package:oasx/service/auto_boot_service.dart`。

注：`main` 的 `addPostFrameCallback` 在首帧后执行，而 `LoginController.onInit` 随初始路由 binding 先运行——此时 `flowActive` 仍为 false。为保证时序正确，`start()` 内把置 `_flowState = _stateRunning` 放在所有 await 之前（上方实现已如此），并把 `hasAutostartArg` 赋值放在 runApp 之前；`LoginController` 的判断改为**直接判触发条件**而非流程状态：

```dart
    final autoBoot =
        Get.isRegistered<AutoBootService>() ? Get.find<AutoBootService>() : null;
    final autoBootActive = autoBoot != null &&
        AutoBootService.shouldAutoBoot(
            hasAutostartArg: autoBoot.hasAutostartArg,
            isDesktop: PlatformUtils.isDesktop,
            entryCount: autoBoot.autoScriptEntries.length);
```

（`flowActive` getter 仍保留供调试；判断用静态条件，消除首帧前后的时序竞态。）

- [ ] **Step 8: killServer 取消调度**

`lib/controller/settings.dart` `killServer()` 成功分支**开头**（`Get.offAllNamed('/login')` 之前）加，确保后续 await 期间不再有延时启动触发：

```dart
      // 先取消自动流程未触发的延时启动（spec §5：killServer 后不再调度），
      // 再走路由跳转与服务注销，避免 await 期间 Timer 到点启动脚本
      if (Get.isRegistered<AutoBootService>()) {
        Get.find<AutoBootService>().cancelScheduled();
      }
```

加 import `package:oasx/service/auto_boot_service.dart`。

- [ ] **Step 9: 全量回归**

Run: `flutter analyze && flutter test`
Expected: analyze 无新增告警；全部测试 PASS

- [ ] **Step 10: 准备 commit**

```
feat(autoboot): 开机自启全自动流程编排与 --autostart 注册参数
```

---

### Task 6: 配置面板改造（server 可达即可配 + 延时输入）与 i18n

**Files:**
- Modify: `lib/views/server/auto_start_settings.dart`（整体改造）
- Modify: `lib/config/translation/i18n_content.dart:241-244`、`lib/config/translation/i18n_cn.dart:92-96`、`lib/config/translation/i18n_us.dart:34-35`
- Test: 重写 `test/views/server/auto_start_settings_test.dart` 的脚本列表部分

**Interfaces:**
- Consumes: Task 2 的 `AutoBootService`（`autoScriptEntries/isSelected/delayOf/setSelected/setDelay`）、`AutoScriptEntry.maxDelaySeconds`
- Produces: `AutoStartSettingsContent({this.probeBackend, this.fetchScripts})`——两个可注入异步函数（默认走 `ApiClient`），widget 测试据此隔离 HTTP

- [ ] **Step 1: i18n 文案**

`lib/config/translation/i18n_content.dart` 替换 `autoRunScriptLoginHint` 区块：

```dart
  // Server 页「OASX自启动设置」区块标题与未连接提示、延时输入、刷新
  static const String oasxAutoStartSettings = 'OASX Autostart Settings';
  static const String autoRunScriptServerHint =
      'Start the server to configure auto-run scripts';
  static const String autoRunScriptDelayLabel = 'Delay (s)';
  static const String autoRunScriptRefresh = 'Refresh';
```

`i18n_cn.dart` 对应替换：

```dart
  I18n.oasxAutoStartSettings: 'OASX自启动设置',
  I18n.autoRunScriptServerHint: '启动 server 后可配置自动启动脚本',
  I18n.autoRunScriptDelayLabel: '延时(秒)',
  I18n.autoRunScriptRefresh: '刷新',
```

`i18n_us.dart` 对应替换（删除 `autoRunScriptLoginHint` 行，加三行新 key，值同 content 英文）。`grep -rn "autoRunScriptLoginHint" lib/ test/` 确认引用全部清理。

- [ ] **Step 2: 重写面板脚本列表区（含探测）**

`lib/views/server/auto_start_settings.dart` 整体替换为：

```dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:get/get.dart';

import 'package:oasx/api/api_client.dart';
import 'package:oasx/config/translation/i18n_content.dart';
import 'package:oasx/model/auto_script_entry.dart';
import 'package:oasx/service/auto_boot_service.dart';
import 'package:oasx/service/auto_start_service.dart';

// 「OASX自启动设置」内容区：开机自启开关 + 自动启动脚本多选与延时配置。
// 脚本列表不再依赖登录（ScriptService），改为探测 server 可达后经
// getScriptList 拉取（spec §4.4）；probeBackend/fetchScripts 可注入以便测试。
class AutoStartSettingsContent extends StatefulWidget {
  const AutoStartSettingsContent(
      {super.key, this.probeBackend, this.fetchScripts});

  // server 可达探测与脚本列表拉取，默认走 ApiClient（探测前按地址规则 set）
  final Future<bool> Function()? probeBackend;
  final Future<List<String>> Function()? fetchScripts;

  @override
  State<AutoStartSettingsContent> createState() =>
      _AutoStartSettingsContentState();
}

class _AutoStartSettingsContentState extends State<AutoStartSettingsContent> {
  // null=探测中；true/false=可达/不可达
  bool? _reachable;
  List<String> _scriptNames = const [];

  @override
  void initState() {
    super.initState();
    _probe();
  }

  // 默认探测实现：按地址取值规则（spec §3）设置全局地址后调 /test。
  // 与登录行为一致；手动登录时会用用户输入值重新覆盖，无负面影响
  Future<bool> _defaultProbe() {
    ApiClient()
        .setAddress('http://${Get.find<AutoBootService>().resolveAddress()}');
    return ApiClient().testAddress();
  }

  Future<void> _probe() async {
    setState(() => _reachable = null);
    final ok = await (widget.probeBackend?.call() ?? _defaultProbe());
    if (!mounted) return;
    if (!ok) {
      setState(() => _reachable = false);
      return;
    }
    final names =
        await (widget.fetchScripts?.call() ?? ApiClient().getScriptList());
    if (!mounted) return;
    setState(() {
      _reachable = true;
      _scriptNames = names;
    });
  }

  @override
  Widget build(BuildContext context) {
    final autoStartService = Get.find<AutoStartService>();

    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        // 开机自启开关；isApplying 时禁用切换，避免并发写系统注册项
        Obx(() {
          final applying = autoStartService.isApplying.value;
          return SwitchListTile(
            title: Text(I18n.launchAtStartup.tr),
            value: autoStartService.enableLaunchAtStartup.value,
            dense: true,
            onChanged: applying
                ? null
                : (v) => autoStartService.updateLaunchAtStartupEnable(v),
          );
        }),
        const Divider(height: 1),
        Row(children: [
          Padding(
            padding: const EdgeInsets.only(left: 16, top: 8, bottom: 4),
            child: Text(I18n.autoRunScriptConfig.tr,
                style: Theme.of(context).textTheme.titleSmall),
          ),
          // 手动刷新：重新探测 + 拉取列表
          IconButton(
            icon: const Icon(Icons.refresh, size: 18),
            tooltip: I18n.autoRunScriptRefresh.tr,
            onPressed: _probe,
          ),
        ]),
        switch (_reachable) {
          // 探测中：占位进度条
          null => const Padding(
              padding: EdgeInsets.all(16),
              child: LinearProgressIndicator(),
            ),
          // 不可达：提示启动 server（spec §4.4，不提供缓存列表）
          false => Padding(
              padding: const EdgeInsets.only(left: 16, bottom: 8),
              child: Text(I18n.autoRunScriptServerHint.tr),
            ),
          true => _buildScriptList(),
        },
      ],
    );
  }

  // 脚本多选 + 每脚本延时输入：勾选绑定 AutoBootService 条目，
  // 延时仅勾选时可编辑，失焦/提交时持久化
  Widget _buildScriptList() {
    final autoBoot = Get.find<AutoBootService>();
    return Obx(() {
      // 读取 autoScriptEntries 建立响应式依赖（勾选/延时变化即重建）
      final entries = {
        for (final e in autoBoot.autoScriptEntries) e.name: e.delaySeconds
      };
      return Column(
        children: _scriptNames.map((name) {
          final selected = entries.containsKey(name);
          return Row(children: [
            Expanded(
              child: CheckboxListTile(
                value: selected,
                onChanged: (v) => autoBoot.setSelected(name, v ?? false),
                title: Text(name),
                dense: true,
                controlAffinity: ListTileControlAffinity.leading,
              ),
            ),
            // 延时输入：0–86400 整数，非法输入由过滤器与 clamp 双重防护
            SizedBox(
              width: 90,
              child: TextFormField(
                key: ValueKey('delay_$name'),
                enabled: selected,
                initialValue: '${entries[name] ?? 0}',
                keyboardType: TextInputType.number,
                inputFormatters: [FilteringTextInputFormatter.digitsOnly],
                decoration: InputDecoration(
                  labelText: I18n.autoRunScriptDelayLabel.tr,
                  isDense: true,
                ),
                onFieldSubmitted: (v) =>
                    autoBoot.setDelay(name, int.tryParse(v) ?? 0),
                onTapOutside: (_) => FocusManager.instance.primaryFocus
                    ?.unfocus(),
                onChanged: (v) =>
                    autoBoot.setDelay(name, int.tryParse(v) ?? 0),
              ),
            ),
            const SizedBox(width: 8),
          ]);
        }).toList(),
      );
    });
  }
}
```

注意：`server_view.dart:148` 的 `children: const [AutoStartSettingsContent()]` 保持 const 可用（构造函数为 const）。

- [ ] **Step 3: 重写 widget 测试**

`test/views/server/auto_start_settings_test.dart`：删除 `_FakeScriptService` 与 ScriptService 相关内容；`pumpContent` 增加注入参数；脚本列表相关用例替换为：

```dart
  // 注入探测结果与脚本列表，隔离真实 HTTP
  Future<void> pumpContent(WidgetTester tester,
      {required bool reachable, List<String> scripts = const []}) async {
    await tester.pumpWidget(MaterialApp(
      home: Scaffold(
        body: SingleChildScrollView(
          child: AutoStartSettingsContent(
            probeBackend: () async => reachable,
            fetchScripts: () async => scripts,
          ),
        ),
      ),
    ));
    // 等待探测 Future 完成
    await tester.pumpAndSettle();
  }

  testWidgets('server 不可达时渲染启动提示、不渲染脚本多选',
      (WidgetTester tester) async {
    await pumpContent(tester, reachable: false);
    expect(find.text(I18n.autoRunScriptServerHint), findsOneWidget);
    expect(find.byType(CheckboxListTile), findsNothing);
    expect(find.byType(SwitchListTile), findsOneWidget);
  });

  testWidgets('可达时渲染脚本列表，勾选持久化新格式并启用延时输入',
      (WidgetTester tester) async {
    await pumpContent(tester, reachable: true, scripts: ['oas1', 'oas2']);
    expect(find.byType(CheckboxListTile), findsNWidgets(2));

    // 勾选 oas1 → AutoBootService 条目新增并按新格式持久化
    await tester.tap(find.text('oas1'));
    await tester.pump();
    final autoBoot = Get.find<AutoBootService>();
    expect(autoBoot.isSelected('oas1'), isTrue);
    expect(jsonDecode(GetStorage().read(StorageKey.autoScriptList.name)), [
      {'name': 'oas1', 'delaySeconds': 0}
    ]);

    // 修改延时 → 持久化延时值
    await tester.enterText(
        find.byKey(const ValueKey('delay_oas1')), '30');
    await tester.pump();
    expect(autoBoot.delayOf('oas1'), 30);

    // 未勾选脚本的延时输入禁用
    final oas2Field = tester.widget<TextFormField>(
        find.byKey(const ValueKey('delay_oas2')));
    expect(oas2Field.enabled, isFalse);
  });
```

setUp 注册 `AutoBootService`（同 Task 4），开机自启开关两个既有用例保留（`pumpContent` 调用处补 `reachable: false`）。

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/views/server/auto_start_settings_test.dart && flutter analyze`
Expected: PASS；无新增告警

- [ ] **Step 5: 准备 commit**

```
feat(ui): 自启配置面板改为 server 可达即可配置并支持每脚本延时
```

---

### Task 7: 全量验证与提交包

**Files:** 无新改动（验证 + 汇总）

- [ ] **Step 1: 全量回归**

Run: `flutter analyze && flutter test`
Expected: analyze 无新增告警；全部测试 PASS

- [ ] **Step 2: 更新 spec 状态行**

`docs/superpowers/specs/2026-07-30-auto-boot-design.md` 状态行更新为「已实施，待 code-reviewer 审查」。

- [ ] **Step 3: 端到端手动验收（用户环境执行）**

1. 关-开一次「开机自启」开关 → `schtasks /Query /TN OASX /XML` 确认 `<Arguments>--autostart</Arguments>`
2. 关闭 server 与 OASX → 命令行 `oasx.exe --autostart` → 观察：server 自动拉起 → 自动进主界面 → 各脚本按延时错峰启动
3. 手动双击 oasx.exe → 自动登录进主界面但脚本不自动启动
4. 未登录状态打开 Server 页 → server 在跑时可见脚本列表可配置延时；server 停时显示提示

- [ ] **Step 4: 逐条向用户展示 6 个 commit message，确认后按 Task 顺序分别提交**

---

## Self-Review 记录

1. **Spec 覆盖**：§3 触发条件/地址规则/平台约束→Task 2/5；§4.1 各文件→Task 2-6；§4.2 时序→Task 5；§4.3 存储→Task 1/2；§4.4 面板→Task 6；§5 错误处理（fail 分支/killServer 取消/到点失败跳过/面板静默）→Task 5/6；§6 验收→Task 7 手动验收；§7 测试→各 Task Step 1。无遗漏。
2. **占位符扫描**：无 TBD/TODO；所有代码步骤含完整代码。
3. **类型一致性**：`AutoScriptEntry(name, delaySeconds)` 位置参构造、`parseStored/encodeList` 静态、`AutoBootService.setSelected/setDelay/removeEntry/isSelected/delayOf/resolveAddress/onScriptServiceReady/cancelScheduled` 签名在 Task 2/4/5/6 引用一致；`ServerLauncher.validatePath` 静态 + `launch()` 实例方法在 Task 3/5 一致。



