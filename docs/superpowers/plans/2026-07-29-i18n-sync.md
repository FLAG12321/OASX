# 后端 I18N 自动补齐 + OASX 即时生效翻译同步 — 实施计划

> **For agentic workers:** Implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 后端新增 task/config 字段后，只需在后端 `assets/i18n/zh-CN.json` 填中文，OASX 无需改代码/重新编译即可当次启动显示翻译。

**Architecture:** 后端服务启动时复用 `script_task()` 与 `gui_menu_list` 收集前端实际渲染的全部翻译 key，把缺失 key 以空值补进 `assets/i18n/zh-CN.json`；`/home/additional_translate` 下发时过滤空值。前端一次性改动：拉取补充翻译后立即合入内存翻译表并刷新 UI（现状是只写缓存、下次启动才生效）。

**Tech Stack:** 后端 Python/FastAPI/pytest；前端 Flutter/GetX/flutter_test。

**Spec:** `docs/superpowers/specs/2026-07-29-i18n-sync-design.md`（OASX 仓库）

## Global Constraints

- 涉及两个仓库：后端 `C:\Users\lu\Desktop\yys\OnmyojiAutoScript-easy-install\OnmyojiAutoScript-easy-install`（Task 1-4，在该目录下运行命令与提交），前端 OASX `C:\Users\lu\Desktop\yys\OnmyojiAutoScript-easy-install\OASX_last\OASX`（Task 5-6）。
- 所有新增/修改代码必须写中文注释（用户全局规则）。
- 每次 `git commit` 前必须先把拟定的 commit message 展示给用户，用户确认后才能执行（用户全局规则）。
- `en-US.json` 不做任何处理（spec 决策：不处理英文）。
- 补齐缺失 key 时必须保留 `assets/i18n/zh-CN.json` 既有条目及其顺序，新 key 追加尾部、值为空字符串 `""`。
- 后端 push 仅用 origin 远程（用户记忆规则）；本计划不含 push，仅本地 commit。
- 路径约定：`assets_i18n_dir` 与既有 `file_zh_cn` 一致，在模块 import 时基于 `Path.cwd()` 固化，要求后端进程以仓库根为工作目录启动（现有部署方式即如此，见 spec §5）。
- 已知限制（spec §5）：后端先于新版 OASX 启动时，镜像陈旧可能产生无害的空值噪音条目，不做处理。

---

### Task 1: 后端 `I18n.collect_frontend_keys` — 收集前端渲染 key 的纯函数

**Files:**
- Modify: `module/server/i18n.py`
- Test: `tests/unit/logic/test_i18n_sync.py`（新建）

**Interfaces:**
- Consumes: 无（纯函数，入参由调用方提供）
- Produces: `I18n.collect_frontend_keys(menu: dict, script_task_fn) -> set`
  - `menu`：`gui_menu_list` 形状 `{菜单分组名: [任务名, ...]}`
  - `script_task_fn`：可调用，`(task_name: str) -> dict[str, list[dict]]`，即 `ConfigModel.script_task` 的形状：`{分组名: [{'name': str, 'description': str(可选), 'enumEnum': list(可选), ...}]}`
  - 返回集合包含：菜单分组名、任务名、args 分组名、字段 `name`、字段 `description`（存在时）、`enumEnum` 中的字符串枚举值

- [ ] **Step 1: 写失败测试**

新建 `tests/unit/logic/test_i18n_sync.py`：

```python
# This Python file uses the following encoding: utf-8
# @brief    后端 i18n 缺失 key 自动补齐的纯逻辑单元测试
#           对应设计文档 docs/superpowers/specs/2026-07-29-i18n-sync-design.md（OASX 仓库）

import json
import pytest

from module.server.i18n import I18n


def fake_script_task(task_name):
    """模拟 ConfigModel.script_task 的返回形状；Broken 任务模拟 schema 异常"""
    if task_name == 'Broken':
        raise ValueError('bad schema')
    return {
        'scheduler': [
            {'name': 'enable', 'type': 'boolean'},
            {'name': 'success_interval',
             'description': 'success_interval_help', 'type': 'string'},
        ],
        'demo_config': [
            {'name': 'battle_mode', 'enumEnum': ['mode_a', 'mode_b'], 'type': 'enum'},
        ],
    }


MENU = {'Daily': ['Demo', 'Broken']}


class TestCollectFrontendKeys:
    def test_collects_all_key_kinds(self):
        # 应收集：菜单分组名/任务名/args 分组名/字段 name/description/enum 值
        keys = I18n.collect_frontend_keys(MENU, fake_script_task)
        assert {'Daily', 'Demo', 'scheduler', 'demo_config',
                'enable', 'success_interval', 'success_interval_help',
                'battle_mode', 'mode_a', 'mode_b'} <= keys

    def test_broken_task_skipped_but_name_kept(self):
        # 单个任务 schema 抛异常：跳过其字段收集，但任务名本身仍收集，且不向外抛
        keys = I18n.collect_frontend_keys(MENU, fake_script_task)
        assert 'Broken' in keys
```

- [ ] **Step 2: 运行测试确认失败**

Run（后端仓库根目录）: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: FAIL，`AttributeError: ... has no attribute 'collect_frontend_keys'`

- [ ] **Step 3: 最小实现**

`module/server/i18n.py` 顶部加 logger 导入：

```python
import json
from pathlib import Path

from module.logger import logger
```

在 `class I18n(Addition):` 内追加方法（放在 `load_zh_cn` 之后）：

```python
    @classmethod
    def collect_frontend_keys(cls, menu: dict, script_task_fn) -> set:
        """收集前端(OASX)渲染时实际会查翻译表的全部 key

        :param menu: gui_menu_list 返回的 {菜单分组名: [任务名]}
        :param script_task_fn: ConfigModel.script_task 的可调用对象
        """
        keys = set()
        for group_name, task_names in menu.items():
            keys.add(group_name)
            for task_name in task_names:
                keys.add(task_name)
                try:
                    task_args = script_task_fn(task_name)
                except Exception as e:
                    # 单个任务 schema 异常只跳过该任务，不影响其余任务收集
                    logger.warning(f'i18n sync: collect keys of task [{task_name}] failed: {e}')
                    continue
                for arg_group, items in task_args.items():
                    keys.add(arg_group)
                    for item in items:
                        # 前端 ArgumentModel.title 取的是 name 字段（不是 title）
                        keys.add(item['name'])
                        if 'description' in item:
                            keys.add(item['description'])
                        for enum_value in item.get('enumEnum', []):
                            if isinstance(enum_value, str):
                                keys.add(enum_value)
        return keys
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 2 passed

- [ ] **Step 5: 提交（先向用户展示 commit message 待确认）**

```bash
git add module/server/i18n.py tests/unit/logic/test_i18n_sync.py
git commit -m "feat(i18n): collect_frontend_keys 收集前端渲染所需翻译 key"
```

---

### Task 2: 后端 `load_additions` 空值过滤 + 路径类属性化

**Files:**
- Modify: `module/server/i18n.py`（`Addition` 类）
- Test: `tests/unit/logic/test_i18n_sync.py`（追加）

**Interfaces:**
- Consumes: 无
- Produces:
  - `Addition.assets_i18n_dir: Path` 类属性（默认 `Path.cwd() / 'assets' / 'i18n'`，测试可 monkeypatch）
  - `Addition.load_additions() -> dict`：返回 `{'en-US': {...}, 'zh-CN': {...}}`，**值为空字符串的条目被过滤**（Task 3 的空值占位 key 不下发）

- [ ] **Step 1: 写失败测试**

在 `tests/unit/logic/test_i18n_sync.py` 追加：

```python
class TestLoadAdditionsFilter:
    def test_empty_values_filtered(self, tmp_path, monkeypatch):
        # 空值占位条目不应下发给前端（前端 .tr 回退显示 key 原文而非空白）
        (tmp_path / 'zh-CN.json').write_text(
            json.dumps({'a': '甲', 'b': ''}, ensure_ascii=False), encoding='utf-8')
        (tmp_path / 'en-US.json').write_text('{}', encoding='utf-8')
        monkeypatch.setattr(I18n, 'assets_i18n_dir', tmp_path)
        data = I18n.load_additions()
        assert data['zh-CN'] == {'a': '甲'}
        assert data['en-US'] == {}

    def test_missing_file_returns_empty(self, tmp_path, monkeypatch):
        # 文件不存在时返回空 dict，不抛异常（保持原行为）
        monkeypatch.setattr(I18n, 'assets_i18n_dir', tmp_path)
        data = I18n.load_additions()
        assert data == {'en-US': {}, 'zh-CN': {}}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 新增 2 个测试红灯——注意 monkeypatch 对不存在的 `assets_i18n_dir` 属性抛 `AttributeError`，pytest 显示为 **ERROR**（而非 FAIL），属预期红灯形态

- [ ] **Step 3: 实现**

`module/server/i18n.py` 中 `Addition` 类改为：

```python
class Addition:
    # 补充翻译目录（下发给 OASX 的翻译源），类属性便于测试替换路径。
    # 注意：与既有 file_zh_cn 一致，import 时基于 Path.cwd() 固化，
    # 要求后端进程以仓库根为工作目录启动
    assets_i18n_dir = Path.cwd() / 'assets' / 'i18n'

    @classmethod
    def load_additions(cls) -> dict:
        result = {}
        files: str = ['en-US', 'zh-CN']
        for file in files:
            file_path = cls.assets_i18n_dir / f'{file}.json'
            result[file] = {}
            if not file_path.exists():
                continue
            with open(str(file_path), 'r', encoding='utf-8') as f:
                data = json.load(f)
            # 过滤空值：sync_missing_keys 写入的占位 key 未填写前不下发，
            # 前端 .tr 查不到会回退显示 key 原文，避免显示空白
            result[file] = {k: v for k, v in data.items() if v}
        return result
```

（`files: str = [...]` 的注解沿用原文件写法，不顺手修改。）

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交（先向用户展示 commit message 待确认）**

```bash
git add module/server/i18n.py tests/unit/logic/test_i18n_sync.py
git commit -m "feat(i18n): additional_translate 下发时过滤空值占位条目"
```

---

### Task 3: 后端 `I18n.sync_missing_keys` — 缺失 key 补齐写回

**Files:**
- Modify: `module/server/i18n.py`（`I18n` 类）
- Test: `tests/unit/logic/test_i18n_sync.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `collect_frontend_keys(menu, script_task_fn)`；Task 2 的 `assets_i18n_dir`；既有 `I18n.file_zh_cn`（镜像文件路径）与 `I18n.load_zh_cn()`
- Produces: `I18n.sync_missing_keys(menu: dict, script_task_fn) -> int`，返回本次补齐的 key 数量；守护分支（镜像缺失/assets JSON 损坏）返回 0 且不写文件。Task 4 在启动钩子中调用。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/logic/test_i18n_sync.py` 追加：

```python
class TestSyncMissingKeys:
    @pytest.fixture
    def i18n_files(self, tmp_path, monkeypatch):
        """搭建镜像文件与 assets 翻译文件的临时环境"""
        mirror = tmp_path / 'mirror' / 'zh-CN.json'
        mirror.parent.mkdir(parents=True)
        # 镜像（前端内置翻译）已覆盖 enable；Demo 同时存在于镜像与 assets（均命中分支）
        mirror.write_text(
            json.dumps({'enable': '启用', 'Demo': '演示任务'}, ensure_ascii=False),
            encoding='utf-8')
        assets_dir = tmp_path / 'assets'
        assets_dir.mkdir()
        # assets 已有 Demo 与 old_key
        (assets_dir / 'zh-CN.json').write_text(
            json.dumps({'Demo': '演示任务', 'old_key': '旧翻译'}, ensure_ascii=False),
            encoding='utf-8')
        monkeypatch.setattr(I18n, 'file_zh_cn', mirror)
        monkeypatch.setattr(I18n, 'assets_i18n_dir', assets_dir)
        return assets_dir / 'zh-CN.json'

    def test_appends_only_missing_keys(self, i18n_files):
        count = I18n.sync_missing_keys(MENU, fake_script_task)
        data = json.loads(i18n_files.read_text(encoding='utf-8'))
        assert count > 0
        # 缺失 key 以空值追加
        assert data['success_interval'] == ''
        assert data['mode_a'] == ''
        # assets 已有条目不动、顺序保留（原有两条仍在最前）
        # Demo 同时存在于 assets 与镜像（均命中），值保持 assets 原文
        assert data['Demo'] == '演示任务'
        assert list(data)[:2] == ['Demo', 'old_key']
        # 镜像已覆盖的 key 不追加
        assert 'enable' not in data

    def test_idempotent_second_run(self, i18n_files):
        # 第二次运行无新缺失，返回 0 且文件不再变化
        I18n.sync_missing_keys(MENU, fake_script_task)
        before = i18n_files.read_text(encoding='utf-8')
        assert I18n.sync_missing_keys(MENU, fake_script_task) == 0
        assert i18n_files.read_text(encoding='utf-8') == before

    def test_skip_when_mirror_missing(self, tmp_path, monkeypatch):
        # 镜像不存在（OASX 从未连接过）：跳过补齐，不创建/修改 assets 文件
        monkeypatch.setattr(I18n, 'file_zh_cn', tmp_path / 'no_mirror.json')
        assets_dir = tmp_path / 'assets'
        assets_dir.mkdir()
        monkeypatch.setattr(I18n, 'assets_i18n_dir', assets_dir)
        assert I18n.sync_missing_keys(MENU, fake_script_task) == 0
        assert not (assets_dir / 'zh-CN.json').exists()

    def test_skip_when_assets_broken(self, i18n_files):
        # assets JSON 损坏：不写回，保护人工翻译成果
        i18n_files.write_text('{broken json', encoding='utf-8')
        assert I18n.sync_missing_keys(MENU, fake_script_task) == 0
        assert i18n_files.read_text(encoding='utf-8') == '{broken json'
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 新增 4 个测试 FAIL，`AttributeError: ... no attribute 'sync_missing_keys'`

- [ ] **Step 3: 实现**

在 `class I18n(Addition):` 内 `collect_frontend_keys` 之后追加：

```python
    @classmethod
    def sync_missing_keys(cls, menu: dict, script_task_fn) -> int:
        """启动时把前端会用到但两侧翻译都缺失的 key 以空值补进 assets/i18n/zh-CN.json

        开发者随后只需在该文件填中文即可，OASX 无需改代码。
        返回本次补齐的 key 数量；守护分支命中时返回 0 且不写文件。
        """
        # 镜像文件（OASX 上传的前端内置翻译）不存在说明前端从未连接过，
        # 无法区分“前端已内置”与“真缺失”，跳过以免灌入大量误报条目
        if not cls.file_zh_cn.exists():
            logger.warning('i18n sync skipped: frontend mirror translation not found')
            return 0
        mirror = cls.load_zh_cn()

        addition_file = cls.assets_i18n_dir / 'zh-CN.json'
        additions = {}
        if addition_file.exists():
            try:
                with open(str(addition_file), 'r', encoding='utf-8') as f:
                    additions = json.load(f)
            except json.JSONDecodeError as e:
                # 文件损坏时不写回，保护人工翻译成果
                logger.error(f'i18n sync skipped: {addition_file} is broken: {e}')
                return 0

        keys = cls.collect_frontend_keys(menu, script_task_fn)
        missing = sorted(k for k in keys if k not in additions and k not in mirror)
        if not missing:
            logger.info('i18n sync: no missing keys')
            return 0
        # 追加到尾部：dict 保序，既有条目顺序不变，占位值为空字符串等待人工填写
        for key in missing:
            additions[key] = ''
        addition_file.parent.mkdir(parents=True, exist_ok=True)
        with open(str(addition_file), 'w', encoding='utf-8') as f:
            json.dump(additions, f, indent=2, ensure_ascii=False)
        logger.info(f'i18n sync: {len(missing)} missing keys appended to {addition_file}')
        return len(missing)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 8 passed

- [ ] **Step 5: 提交（先向用户展示 commit message 待确认）**

```bash
git add module/server/i18n.py tests/unit/logic/test_i18n_sync.py
git commit -m "feat(i18n): sync_missing_keys 启动时自动补齐缺失翻译 key"
```

---

### Task 4: 后端启动钩子挂接

**Files:**
- Modify: `module/server/app.py:77-84`（`on_startup`）

**Interfaces:**
- Consumes: Task 3 的 `I18n.sync_missing_keys(menu, script_task_fn)`；既有 `mm.config_cache('template')`（返回 `Config`，含 `gui_menu_list` 属性与 `model.script_task` 方法）
- Produces: 服务启动时自动执行补齐；无新接口

- [ ] **Step 1: 修改 `on_startup`**

`module/server/app.py` 导入区追加：

```python
from module.server.i18n import I18n
```

`on_startup` 改为：

```python
async def on_startup():
    """
    app.state 的生命周期在定义app的时候就有了
    :return:
    """
    logger.info('OAS web service startup done')
    # 启动时扫描 template 配置，把前端会用到但缺失翻译的 key 补进 assets/i18n/zh-CN.json；
    # 任何异常只记日志，不阻塞服务启动
    try:
        template = mm.config_cache('template')
        I18n.sync_missing_keys(template.gui_menu_list, template.model.script_task)
    except Exception as e:
        logger.error(f'i18n sync failed: {e}')
    if app.state.script_instances:
        await mm.restart_processes(app.state.script_instances)
```

- [ ] **Step 2: 冒烟验证（不依赖启动完整服务）**

Run（后端仓库根目录）:

```bash
python -c "import time; from module.server.main_manager import mm; from module.server.i18n import I18n; t = mm.config_cache('template'); s = time.time(); print('appended:', I18n.sync_missing_keys(t.gui_menu_list, t.model.script_task)); print('elapsed(s):', round(time.time() - s, 2))"
```

Expected: 打印 `appended: <数量>`（首次可能 >0）与耗时（对 80+ 任务全量提取 schema，应在数秒内，作为启动延迟量级记录）；且 `git diff assets/i18n/zh-CN.json` 显示只在尾部追加了空值条目、既有条目未变。若打印 warning `frontend mirror translation not found`，说明本机 `module/config/i18n/zh-CN.json` 缺失，属守护分支正常行为。

- [ ] **Step 3: 回归全部单元测试**

Run: `python -m pytest tests/unit/logic/test_i18n_sync.py -v`
Expected: 8 passed

- [ ] **Step 4: 提交（先向用户展示 commit message 待确认；`assets/i18n/zh-CN.json` 若因冒烟验证产生追加条目，一并提交或按用户意见回退）**

```bash
git add module/server/app.py
git commit -m "feat(server): 启动时自动补齐 i18n 缺失 key"
```

---

### Task 5: 前端 `applyAdditionalTranslate` — 合入内存翻译表

**仓库切换：以下在 OASX 仓库执行。**

**Files:**
- Modify: `lib/service/locale_service.dart`（`DynamicMessages` mixin）
- Modify: `lib/config/translation/i18n.dart:96-103`（`translateUpdate` 空值守护）
- Test: `test/service/locale_service_apply_test.dart`（新建）

**Interfaces:**
- Consumes: 既有 `Messages.translateUpdate(String key, String value, {String locale})`（`lib/config/translation/i18n.dart:96`）
- Produces:
  - `DynamicMessages.applyAdditionalTranslate(Map<String, Map<String, String>> data)`，`data` 形状同 `ApiClient.getAdditionalTranslate()` 返回值：`{'zh-CN': {...}, 'en-US': {...}}`。Task 6 调用。
  - `translateUpdate` 行为变化：value 为空字符串时忽略并返回 `false`（兼容旧后端下发空值与历史缓存残留空值，`loadMessage` 路径同样受保护）

- [ ] **Step 1: 写失败测试**

新建 `test/service/locale_service_apply_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:oasx/service/locale_service.dart';

/// 仅混入 DynamicMessages，绕开 LocaleService 对 GetStorage 的依赖
class _FakeService with DynamicMessages {}

void main() {
  test('applyAdditionalTranslate 立即合入内存翻译表', () {
    final service = _FakeService();
    service.applyAdditionalTranslate({
      'zh-CN': {'new_task_key': '新任务'},
      'en-US': {'new_task_key': 'New Task'},
    });
    expect(service.messages.all_cn_translate['new_task_key'], '新任务');
    expect(service.messages.all_us_translate['new_task_key'], 'New Task');
  });

  test('translateUpdate 忽略空字符串值（旧后端/旧缓存兼容）', () {
    final service = _FakeService();
    service.applyAdditionalTranslate({
      'zh-CN': {'empty_key': ''},
      'en-US': {},
    });
    // 空值不进入翻译表，.tr 回退显示 key 原文而非空白
    expect(service.messages.all_cn_translate.containsKey('empty_key'), false);
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

Run（OASX 仓库根目录）: `flutter test test/service/locale_service_apply_test.dart`
Expected: 编译错误 `applyAdditionalTranslate isn't defined`

- [ ] **Step 3: 实现**

`lib/config/translation/i18n.dart` 的 `translateUpdate` 开头加空值守护：

```dart
  bool translateUpdate(String key, String value, {String locale = 'zh-CN'}) {
    // 空字符串守护：旧后端下发或历史缓存中的空值占位条目直接忽略，
    // 否则 .tr 返回空串会导致界面显示空白
    if (value.isEmpty) {
      return false;
    }
    if (locale == 'en-US') {
      all_us_translate[key] = value;
    } else if (locale == 'zh-CN') {
      all_cn_translate[key] = value;
    }
    return true;
  }
```

`lib/service/locale_service.dart` 的 `mixin DynamicMessages` 内、`saveAdditionalTranslate` 之前追加：

```dart
  /// 把后端下发的补充翻译立即合入内存翻译表，使当次启动即可显示新翻译
  /// （saveAdditionalTranslate 只写缓存文件，要下次启动才生效）
  ///
  /// 注意隐式契约：putChineseTranslate 上传镜像用的是 Messages() 新实例
  /// （编译期内置翻译），不受本方法运行时合入的条目影响；后端靠这一点
  /// 区分「前端内置」与「真缺失」，勿改为复用本实例上传
  void applyAdditionalTranslate(Map<String, Map<String, String>> data) {
    data.forEach((lang, entries) {
      entries.forEach(
          (key, value) => _messages.translateUpdate(key, value, locale: lang));
    });
  }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/service/locale_service_apply_test.dart`
Expected: All tests passed

- [ ] **Step 5: 提交（先向用户展示 commit message 待确认）**

```bash
git add lib/service/locale_service.dart lib/config/translation/i18n.dart test/service/locale_service_apply_test.dart
git commit -m "feat(i18n): applyAdditionalTranslate 将补充翻译合入内存翻译表"
```

---

### Task 6: 前端启动流程接线 — 拉取后立即生效

**Files:**
- Modify: `lib/controller/ctrl_nav.dart:19-31`（`NavCtrl.onInit`）

**Interfaces:**
- Consumes: Task 5 的 `applyAdditionalTranslate`；既有 `ApiClient().getAdditionalTranslate()`、`LocaleService.saveAdditionalTranslate`、GetX `Get.updateLocale`
- Produces: 无新接口；行为变化 = 补充翻译当次启动立即生效

- [ ] **Step 1: 修改 `NavCtrl.onInit`**

`lib/controller/ctrl_nav.dart` 中：

```dart
  @override
  Future<void> onInit() async {
    await ApiClient().putChineseTranslate();
    final additionalTranslate = await ApiClient().getAdditionalTranslate();
    if (additionalTranslate.isNotEmpty) {
      final localeService = Get.find<LocaleService>();
      // 先合入内存翻译表（当次启动立即生效），再写缓存文件（离线兜底，下次启动早期加载）
      localeService.applyAdditionalTranslate(additionalTranslate);
      await localeService.saveAdditionalTranslate(additionalTranslate);
      // 此时部分界面可能已用旧翻译渲染，强制触发一次全局重建
      if (Get.locale != null) {
        Get.updateLocale(Get.locale!);
      }
    }

    navNameList.value = await ApiClient().getConfigList();
    homeMenuJson.value = await ApiClient().getHomeMenu();
    scriptMenuJson.value = await ApiClient().getScriptMenu();

    super.onInit();
  }
```

- [ ] **Step 2: 静态检查与全量前端测试**

Run: `flutter analyze lib/controller/ctrl_nav.dart lib/service/locale_service.dart && flutter test`
Expected: analyze 无新增告警；既有测试全部通过

- [ ] **Step 3: 提交（先向用户展示 commit message 待确认）**

```bash
git add lib/controller/ctrl_nav.dart
git commit -m "feat(i18n): 补充翻译拉取后当次启动立即生效"
```

---

### Task 7: 端到端手动验收（spec 第 6 节）

**Files:** 无代码改动；两端联调。

- [ ] **Step 1: 启动后端服务**（后端仓库，按项目常规方式如 `oas-backend.bat` 或 `python server.py`），确认日志出现 `i18n sync: ... missing keys appended` 或 `no missing keys`，且 `assets/i18n/zh-CN.json` 既有条目未被改动。
- [ ] **Step 2:** 在 `assets/i18n/zh-CN.json` 中挑一个新追加的空值 key 填入中文（**不重启后端**）。
- [ ] **Step 3:** 启动 OASX（开发模式 `flutter run` 即可），登录后确认该 key 对应的菜单/字段**当次启动内**显示刚填的中文，无需重启 OASX。
- [ ] **Step 4:** 确认某个仍为空值的 key 在 OASX 显示英文原 key（非空白）。
- [ ] **Step 5:** 切换到英文语言，确认补充翻译的 key **回退显示中文**（GetX `.tr` 查不到时回退 fallbackLocale zh-CN，spec §3 已声明的预期行为，非 bug），切回中文。
- [ ] **Step 6（AC1/AC6 增量工作流验证）:** 临时给某个任务的 pydantic config model 加一个探针字段，例如 `i18n_sync_probe: bool = Field(default=False, description='i18n_sync_probe_help')` → 重启后端 → 确认 `assets/i18n/zh-CN.json` 尾部出现 `i18n_sync_probe` 与 `i18n_sync_probe_help` 空值条目 → 给 `i18n_sync_probe` 填入中文 → 启动 OASX 确认该字段当次显示中文且前端零改动 → 验毕撤销探针字段、并删除 assets 文件中这两个探针条目。
- [ ] **Step 7:** 对照 spec 第 6 节验收标准逐条确认，向用户报告结果。

---

## Self-Review 记录

- Spec 覆盖：4.1→Task 1/3/4，4.2→Task 2，4.3→Task 5/6，错误处理（§5）→Task 1 broken-task 测试、Task 3 守护分支测试、Task 4 try/except、Task 6 `Get.locale` 判空，验收（§6）→Task 7，测试（§7）→各 Task 的 TDD 步骤。无遗漏。
- 占位符扫描：无 TBD/TODO/“适当处理”类表述；所有代码步骤含完整代码。
- 类型一致性：`collect_frontend_keys(menu, script_task_fn)` 与 `sync_missing_keys(menu, script_task_fn)` 签名在 Task 1/3/4 一致；`assets_i18n_dir` 在 Task 2 定义、Task 3 使用；`applyAdditionalTranslate` 在 Task 5 定义、Task 6 调用，参数形状与 `getAdditionalTranslate()` 返回值一致。

## 对抗审查记录（Gate 2）

- plan-reviewer（opus，隔离上下文）：CONDITIONAL PASS，3 条 MEDIUM 缺陷已全部修复——①en-US 回退行为断言错误（spec §3 与 Task 7 Step 5 已改为「回退显示中文」）；②前端空值防御缺失（Task 5 增加 `translateUpdate` 空值守护及测试）；③`assets_i18n_dir` 求值时机（Global Constraints 与 spec §5 已声明）。建议项同步采纳：Task 2 Step 2 红灯形态改为 ERROR、Task 3 补「两侧均命中」用例、镜像陈旧记为已知限制、Task 4 冒烟记录耗时。
- design-sync（sonnet，隔离上下文）：追溯矩阵全覆盖；D1（AC1/AC6 无具体 E2E 步骤）→ Task 7 新增 Step 6 探针字段验证；D3 同 plan-reviewer 建议已修；D4（§8 审查流程归属）→ 由外层 adversarial-review-gates 工作流承担（Gate 2 计划审查 + Gate 3/4 代码审查），不在本 Plan 内。
- technical-designer（sonnet，隔离上下文，第二轮）：架构级 **PASS**。采纳 O3（`applyAdditionalTranslate` 注释声明镜像纯净性契约）；O1（内置 key 与下发 key 的 en-US 行为不一致）为用户决策接受的已知行为；O2（缺失 key 按字母序追加、不按任务聚簇）记为未来可选改进，本次不做。
